<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/nav_graph"
        app:startDestination="@id/LaunchFragment">
    <fragment
            android:id="@+id/LaunchFragment"
            android:name="com.mimm.geoskill.PrepairFragment"
            android:label="@string/ct_name"
            tools:layout="@layout/prepair_fragment">
        <action
                android:id="@+id/action_NameFragment_to_TestFragment"
                app:destination="@id/TestFragment" />
    </fragment>
        <fragment
            android:id="@+id/TestFragment"
            android:name="com.mimm.geoskill.TestFragment"
            android:label="@string/quiz"
            tools:layout="@layout/fragment_test"/>
</navigation>